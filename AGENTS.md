# KAKU AI STUDIO v1

このリポジトリでは、AIが参考サイト調査、設計、実装、検証、公開準備を連続して行う。

## 変更時の必須処理
1. `studio/references` と `studio/brands` を読む。
2. 参考サイトをコピーせず、原理をブランドへ変換する。
3. 実装後に `npm run check`。
4. 1440px と 390px のスクリーンショットを確認。
5. ポインタ・タップ・スマホタッチを検証。
6. ビルド失敗時は公開しない。
7. 完了報告には、変更内容・検証結果・公開URLのみを書く。

## 簡単な指示の例
- 「墨をゆっくりに」: 減衰、拡散、入力速度係数を調整し、PC/スマホ双方を再検証。
- 「作品追加」: worksデータへ追加し、レイアウト崩れを確認。
- 「このURLを参考に」: referenceフォルダを新設し、検証事実と推定を分けて保存。
